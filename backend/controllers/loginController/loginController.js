const loginController = (req, res) => {
    // Lógica para autenticar al usuario y generar el token JWT
    // Devuelve la respuesta apropiada, como el token JWT o un mensaje de error
  };
  
  module.exports = loginController;
  